language: csharp
solution: source/platform/platform.sln
mono:
  - latest
install:
  - travis_retry npm install
before_script:
  - npm prune
jobs:
  include:
    - stage: release
      node_js: lts/*
      script:
        - npm run semantic-release
