language: csharp
solution: source/platform/platform.sln
mono:
  - latest
install:
  - travis_retry npm install
jobs:
  include:
    - stage: release
      node-js:
        - 8
      script:
        - npm run semantic-release
